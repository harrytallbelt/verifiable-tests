i, min_i := 1, 0;
do
    i <> N-1  ->  if
                      a[i] < a[min_i]   ->  min_i := i
                    | a[i] >= a[min_i]  ->  skip
                  fi;
                  i := i + 1
od
