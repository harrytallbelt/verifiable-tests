const parser = require('./parser')

function verify(task, code) {
  const precondition = null       // To get these
  const postcondition = null      // we will have
  const invariants = null         // to implement
  const boundaryFunctions = null  // predicate parsing.

  // TODO: verification-script.md also says
  // about a list of Simplify definitions.

  
  

  // TODO: remove
  return { errors: [] }
}

module.exports = verify